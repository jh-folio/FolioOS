name: CI

on:
  push:
    branches:
      - master
      - main
  pull_request:

permissions:
  contents: read

jobs:
  python-web:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2022, macos-14, ubuntu-24.04]
    env:
      UV_NO_CONFIG: "1"
      GITLEAKS_VERSION: "8.30.1"
      GITLEAKS_WINDOWS_SHA256: d29144deff3a68aa93ced33dddf84b7fdc26070add4aa0f4513094c8332afc4e
      GITLEAKS_LINUX_SHA256: 551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb
      GITLEAKS_DARWIN_X64_SHA256: dfe101a4db2255fc85120ac7f3d25e4342c3c20cf749f2c20a18081af1952709
      GITLEAKS_DARWIN_ARM64_SHA256: b40ab0ae55c505963e365f271a8d3846efbc170aa17f2607f13df610a9aeb6a5

    steps:
      - name: Check out the triggering commit with full history
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Set up Python 3.12.10
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12.10"

      - name: Set up Node 22.17.0
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: "22.17.0"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Regenerate runtime and test locks twice, normalize LF, and byte-compare
        run: |
          python -m pip install --require-hashes --no-deps -r requirements-bootstrap.lock.txt
          python -c "from shutil import copyfile; copyfile('requirements.lock.py312.txt', 'requirements.lock.py312.committed.txt')"
          python -c "from shutil import copyfile; copyfile('requirements-test.lock.py312.txt', 'requirements-test.lock.py312.committed.txt')"
          python -m uv pip compile --universal --python-version 3.12 --resolution highest --generate-hashes --exclude-newer 2026-07-16T00:00:00Z --output-file requirements.lock.py312.txt requirements.txt
          python -m uv pip compile --universal --python-version 3.12 --resolution highest --generate-hashes --exclude-newer 2026-07-16T00:00:00Z --output-file requirements-test.lock.py312.txt requirements-test.txt
          python scripts/normalize_lock_lf.py requirements.lock.py312.txt
          python scripts/normalize_lock_lf.py requirements-test.lock.py312.txt
          python -c "from shutil import copyfile; copyfile('requirements.lock.py312.txt', 'requirements.lock.py312.first.txt')"
          python -c "from shutil import copyfile; copyfile('requirements-test.lock.py312.txt', 'requirements-test.lock.py312.first.txt')"
          python -m uv pip compile --universal --python-version 3.12 --resolution highest --generate-hashes --exclude-newer 2026-07-16T00:00:00Z --output-file requirements.lock.py312.txt requirements.txt
          python -m uv pip compile --universal --python-version 3.12 --resolution highest --generate-hashes --exclude-newer 2026-07-16T00:00:00Z --output-file requirements-test.lock.py312.txt requirements-test.txt
          python scripts/normalize_lock_lf.py requirements.lock.py312.txt
          python scripts/normalize_lock_lf.py requirements-test.lock.py312.txt
          python -c "from shutil import copyfile; copyfile('requirements.lock.py312.txt', 'requirements.lock.py312.second.txt')"
          python -c "from shutil import copyfile; copyfile('requirements-test.lock.py312.txt', 'requirements-test.lock.py312.second.txt')"
          python scripts/compare_lock_bytes.py requirements.lock.py312.committed.txt requirements.lock.py312.first.txt requirements.lock.py312.second.txt
          python scripts/compare_lock_bytes.py requirements-test.lock.py312.committed.txt requirements-test.lock.py312.first.txt requirements-test.lock.py312.second.txt
          python -m uv pip install --system --require-hashes -r requirements-test.lock.py312.txt

      - name: Install checksum-verified Gitleaks 8.30.1
        run: |
          python scripts/install_gitleaks.py --version 8.30.1 --verify-checksum
          python -c "import os; from pathlib import Path; path=Path('.tools/gitleaks/8.30.1').resolve(); github_path=Path(os.environ['GITHUB_PATH']); github_path.write_text(github_path.read_text(encoding='utf-8') + str(path) + '\n', encoding='utf-8')"

      - name: Scan full Git history
        run: gitleaks git --redact=100 --report-format json --report-path "${{ runner.temp }}/gitleaks-history.json"

      - name: Compile app
        run: python -m py_compile app.py

      - name: Audit public release tree
        run: python scripts/public_release_audit.py

      - name: Validate release package manifest
        run: python scripts/package_release.py --dry-run

      - name: Run Python tests
        run: python -m pytest features tests -q

      - name: Install web dependencies
        run: npm ci
        working-directory: web

      - name: Typecheck web app
        run: npm run typecheck
        working-directory: web

      - name: Test web app
        run: npm test
        working-directory: web

      - name: Build web app and reject bundle drift
        working-directory: web
        run: |
          npm run build
          git diff --exit-code -- ../public/react
          git status --porcelain --untracked-files=all -- ../public/react
          python -c "import subprocess,sys; output=subprocess.check_output(['git','status','--porcelain','--untracked-files=all','--','../public/react'], text=True); print(output, end=''); sys.exit(bool(output.strip()))"

      - name: Install Chromium for UI quality gate
        if: matrix.os == 'ubuntu-24.04'
        run: npx playwright install --with-deps chromium
        working-directory: web

      - name: Run theme, responsive, and accessibility quality gate
        if: matrix.os == 'ubuntu-24.04'
        run: npm run test:ui
        working-directory: web

  release-package:
    name: Canonical Ubuntu release package
    needs: [python-web]
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      GITLEAKS_VERSION: "8.30.1"
      GITLEAKS_LINUX_SHA256: 551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb

    steps:
      - name: Check out the exact tested commit
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Set up Python 3.12.10
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12.10"

      - name: Install checksum-verified Gitleaks 8.30.1
        run: |
          python scripts/install_gitleaks.py --version 8.30.1 --verify-checksum
          python -c "import os; from pathlib import Path; path=Path('.tools/gitleaks/8.30.1').resolve(); github_path=Path(os.environ['GITHUB_PATH']); github_path.write_text(github_path.read_text(encoding='utf-8') + str(path) + '\n', encoding='utf-8')"

      - name: Build and verify canonical source-only package
        run: |
          python scripts/package_release.py --output dist/release
          python scripts/verify_release.py --release-dir dist/release/FolioOS-v0.3.0 --expected-commit "${{ github.sha }}"
          gitleaks dir dist/release/FolioOS-v0.3.0 --redact=100 --report-format json --report-path "${{ runner.temp }}/gitleaks-artifact.json"

      - name: Upload the sole canonical release artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: FolioOS-v0.3.0
          path: dist/release/FolioOS-v0.3.0.zip
          if-no-files-found: error
